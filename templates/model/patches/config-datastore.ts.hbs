    {{datastore}}: {
{{#if isMongoDb}}        type: "mongodb",
        host: "localhost",
        database: "{{project_name}}",
{{/if}}{{#if isPostgreSql}}        type: "postgres",
        host: "localhost",
        port: 5432,
        database: "{{project_name}}",
        username: "postgres",
        password: "postgres",
        synchronize: true,
{{/if}}{{#if isSqlite}}        type: "better-sqlite3",
        host: "localhost",
        database: "./data/{{project_name}}.db",
        synchronize: true,
{{/if}}    },
