---
apiVersion: v1
kind: Service
metadata:
  name: [[name]]-services
  labels:
    app: [[name]]
    app.kubernetes.io/instance: [[name]]-services
spec:
  ports:
    - port: 80
      targetPort: 3000
      name: nodejs
      protocol: TCP
  selector:
    app: [[name]]