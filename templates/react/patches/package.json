{
  "scripts": {
    "build": "rimraf dist && tsc && tsc -p tsconfig.client.json && vite build",
    "export": "rapidrest react export",
    "lint": "eslint ./apps ./src ./test"
  },
  "dependencies": { "@rapidrest/react": "^1.0.0", "react": "^19.2.8", "react-dom": "^19.2.8" },
  "devDependencies": { "@vitejs/plugin-react": "^6.1.0", "vite": "^8.2.2" }
}
