    {{datastore}}: {
{{#if isSql}}{{#if isPostgreSql}}        type: "postgres",
        host: "localhost",
        port: 5432,
        database: "{{project_name}}",
        username: "postgres",
        password: "postgres",
        synchronize: true,
{{/if}}{{#if isSqlite}}        type: "better-sqlite3",
        host: "localhost",
        database: "./data/{{project_name}}-auth.db",
        synchronize: true,
{{/if}}{{else}}        type: "mongodb",
        host: "localhost",
        database: "{{project_name}}-auth",
        synchronize: true,
{{/if}}    },
