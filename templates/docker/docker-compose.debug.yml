services:
    server:
{{#if pkgMgr.yarn}}
        command: ["yarn", "debug", "--docker"]
{{else}}
        command: ["npm", "run", "debug", "--", "--docker"]
{{/if}}
        volumes:
            - ./app:/app/app
            - ./src:/app/src
